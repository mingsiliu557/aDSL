# SF27 fTetWild 900 秒补测

一次补测于 2026-09-13 15:51:53 UTC 结束：**TIMEOUT，900.041 秒，SIGTERM / exit -15**。
原始木结模型、输入文件、尺度、0.1 mm envelope、质量目标 10、80 次优化上限、2 线程和 6 GiB 地址空间限制保持不变，仅生成预算从 300 秒改为 900 秒。输入与执行源码哈希核对一致。

预处理 44.42 秒，初始四面体化 0.039 秒，首次三角形插入/切分 77.99 秒。后续仍在优化，而非已证实死锁：

| 日志轮次（从0计数） | 最后记录的最大 AMIPS | 中间四面体数 |
|---|---:|---:|
| 0 | 40719 | 143191 |
| 3 | 322.111 | 126216 |
| 9 | 81.8508 | 707575 |
| 13（未完成） | 25.587 | 1023333 |

最后停在 vertex smoothing；日志中间四面体计数最高为 1,332,707。
这些是优化过程中的计数，不是最终过滤输出。没有最终网格，因此无效单元、
边界偏差、联合体积和连接保持性均未验证；没有追加 CalculiX 求解或继续延长预算。

SF20/adsl 确为原计划选定的真实间隙对照：原源码立柱顶端 3.225、上部颈件底端
3.230，相差 0.005 源码单位；原 OCC 报告确认属于不同实体分量。在原 FEA
尺度下间隙为 2.03821656 mm。本轮仅核对已有记录，没有重跑。其 fTetWild
输出间隙保持性仍未验证。

结论：当前成本和未完成的几何验证不支持把这一路径放进 agent 的默认循环。
这不证明 fTetWild 永远无法成功。按用户决定，生产保留 OCC → Gmsh → CalculiX，
以及已有超时、反馈、资产保存与候选回归保护；不接入自动网格后端切换。

本地产物（不随源码上传）：`local_experiment/ftetwild_sf27_900_20260913/`，
包含 `mesh.log`、`mesh_process.json`、`completion.json`、`provenance.json`、
`sf20_identity_check.json` 及原始输入副本。
